# TRADER_NOTES.md — ملاحظات تسليم منصة تقييم فعالية التاجر الصغير

> يُنتَج في نهاية Phase 7 (Polish & Deploy) حسب Section P من ملف التعليمات. هذا الملف لك أنت (إبراهيم) — دليل تشغيلي سريع، لا مستند تقني.

---

## ١. الروابط الحية (بعد الدفع لـ main فقط — لم يحدث push بعد)

| الرابط | الوصف |
|---|---|
| `https://bare-academy.vercel.app/trader` | بوابة الدخول (مدير أو مقيّم) |
| `https://bare-academy.vercel.app/trader/admin` | لوحة الإدارة (بعد تسجيل دخول مدير) |
| `https://bare-academy.vercel.app/trader/evaluator` | قائمة المتاجر (بعد تسجيل دخول مقيّم) |
| `https://bare-academy.vercel.app/view` | لوحة الصدارة العامة — بدون تسجيل دخول، تُعرض على شاشة الفعالية |

**⚠️ هذه الروابط لن تعمل بعد حتى تُنفَّذ خطوتان يدويتان (انظر §٦ أدناه): تشغيل `trader-schema.sql` في Supabase + الدفع الفعلي لـ main.**

---

## ٢. بيانات الدخول الافتراضية

- **اسم المستخدم:** `admin`
- **كلمة المرور:** موجودة بالنص الصريح في ملف `trader-schema.sql` المحلي على جهازك (جذر مستودع bare-academy) — لم تُكتب هنا عمداً، ولا في أي ملف آخر مُلتزَم بـ Git، لتفادي نشرها عبر HTTP.
- **⚠️ تحديث حرج من مراجعة Phase 7:** القيمة الافتراضية المستخدَمة كـ seed password لهذا الحساب **ليست سرّاً فعلياً من الأساس** — هي نفس كلمة مرور `dashboard.html` القديمة، والموثَّقة والمنشورة أصلاً في `README.md` (ملف مُلتزَم بـ Git، يُخدَّم علناً عبر HTTP لأن الموقع static بلا build step). أي شخص يعرف رابط `bare-academy.vercel.app/README.md` يعرف كلمة مرور admin الافتراضية لمنصة trader، بغض النظر عن أي حجب في ملفات التوثيق الأخرى.
- **بعد أول تسجيل دخول ناجح:** تغيير كلمة مرور `admin` من لوحة الإدارة (تبويب المستخدمون → تعديل) لم يعد "يُنصَح به بشدة" فقط — **أصبح إلزامياً وعاجلاً** قبل أي استخدام حقيقي أو تسليم رابط `/trader/admin` لأي طرف ثالث، تحديداً بسبب النقطة أعلاه.

---

## ٣. دليل سريع (٥ خطوات)

### ١) كيفية إنشاء مقيّم جديد
سجّل دخول كـ `admin` → `/trader/admin` → تبويب "المستخدمون" → زر "+ إضافة مقيّم" → أدخل اسم المستخدم والاسم الكامل → اضغط "توليد" لكلمة مرور تلقائية (أو اكتب واحدة) → اختر الدور "مقيّم" → "حفظ". كلمة المرور تظهر بالنص الصريح في الجدول — أرسلها للمقيّم مباشرة (رسالة واتساب مثلاً).

### ٢) كيفية إضافة/تعديل معيار تقييم
`/trader/admin` → تبويب "المعايير" → "+ إضافة معيار" (أو زر "تعديل" على معيار موجود) → اكتب الاسم، الحد الأقصى للدرجة (١-١٠)، والترتيب → "حفظ". يمكن تعطيل معيار مؤقتاً بالضغط على شارة "مفعّل/معطّل" بدل حذفه — المعايير المعطَّلة لا تظهر لأي مقيّم عند التقييم.

### ٣) كيفية إضافة متجر
من لوحة الإدارة (تبويب "المتاجر") **أو** من تطبيق المقيّم مباشرة (`/trader/evaluator` → "+ إضافة متجر") → أدخل الاسم الأول واسم العائلة (النوع اختياري) → إن أضفته من تطبيق المقيّم يمكنك اختيار "إضافة والتقييم" للانتقال مباشرة لتقييمه.

### ٤) كيفية مشاركة رابط لوحة الصدارة على شاشة الفعالية
افتح `https://bare-academy.vercel.app/view` على أي جهاز عرض/تلفاز/بروجكتر متصل بالإنترنت. لا حاجة لتسجيل دخول. الصفحة تُحدَّث تلقائياً كل ١٠ ثوانٍ، وفيها زر "🔄 تحديث يدوي" لو أردت تحديثاً فورياً.

### ٥) كيفية إعلان الفائز
من نفس صفحة `/view`، بعد وجود تقييم واحد على الأقل، يظهر زر "👑 إعلان الفائز" في الشريط العلوي — اضغطه لعرض بطاقة احتفالية بالمتجر الأول (بالمجموع الأعلى) مع confetti. يُغلَق بالضغط على "إغلاق" أو مفتاح Escape أو النقر خارج البطاقة.

---

## ٤. المقايضات الموثَّقة (بقرارك المعتمد في Phase 1 — القرار ج)

- **كلمات المرور نصية (plaintext):** لا تشفير. موجودة بالنص الصريح في جدول `trader_users` وتظهر بالنص الصريح في لوحة الإدارة. قرار مقصود لسهولة الإدارة اليدوية لعدد صغير من المستخدمين المعروفين شخصياً.
- **RLS مفتوحة (permissive):** أي شخص يملك مفتاح anon (مكشوف أصلاً في كل ملفات bare-academy) يقدر تقنياً يقرأ/يكتب مباشرة في جداول `trader_*` بدون المرور عبر تسجيل الدخول في `/trader`. مقبول لسياق فعالية داخلية مؤقتة.
- **لا "نسيت كلمة المرور":** لا يوجد مسار استرجاع تلقائي. لو نسي مقيّم كلمة مروره، المدير يفتحها له مباشرة من لوحة الإدارة (تظهر بالنص الصريح). لو فُقد آخر حساب `admin`، الاستعادة تتطلب تدخلاً يدوياً في Supabase Studio (تعديل صف مباشرة في جدول `trader_users`) — لوحة الإدارة نفسها تمنع حذف آخر admin أو حذف حسابك الحالي (حراسة أُضيفت في Phase 4).
- **لا خصوصية أبعد من التجميع في `/view`:** الصفحة العامة تعرض متوسطات فقط، ولا تجلب هوية المقيّمين تقنياً (تحقَّق من ذلك مرتين — Phase 6 ثم مراجعة Phase 7 الشاملة)، لكن هذا حماية من التسريب العرضي فقط — ليس حماية مطلقة ضد شخص يستخدم مفتاح anon مباشرة (نفس القيد أعلاه).
- **كلمة مرور admin الافتراضية ليست سرّاً فعلياً (مُكتشَف في Phase 7):** انظر §٢ أعلاه — إعادة استخدام كلمة مرور dashboard القديمة المنشورة في README.md تعني أن التغيير الفوري بعد أول دخول ليس مقايضة اختيارية، بل خطوة تشغيلية مطلوبة.

---

## ٥. قائمة كل ملفات التوثيق (بالترتيب الزمني)

| الملف | المرحلة |
|---|---|
| `plan-phase-1-foundation.html` | Phase 1 — الخطة |
| `report-phase-1-foundation.html` | Phase 1 — التقرير |
| `review-2026-08-05-phase-1.html` | Phase 1 — المراجعة (مُعدَّل في Phase 7: حجب تسريب كلمة مرور اكتُشف في نص المراجعة نفسه) |
| `plan-phase-2-shared-libraries.html` | Phase 2 — خطة/تقرير مدمجان |
| `review-2026-08-05-phase-2.html` | Phase 2 — المراجعة |
| `plan-phase-3-login-router.html` | Phase 3 — خطة/تقرير مدمجان |
| `review-2026-08-05-phase-3.html` | Phase 3 — المراجعة (مُعدَّل في Phase 7: نفس نوع الحجب) |
| `plan-phase-4-admin-dashboard.html` | Phase 4 — خطة/تقرير مدمجان |
| `review-2026-08-05-phase-4.html` | Phase 4 — المراجعة |
| `plan-phase-5-evaluator-app.html` | Phase 5 — خطة/تقرير مدمجان |
| `review-2026-08-05-phase-5.html` | Phase 5 — المراجعة |
| `plan-phase-6-public-leaderboard.html` | Phase 6 — خطة/تقرير مدمجان |
| `review-2026-08-05-phase-6.html` | Phase 6 — المراجعة |
| `report-phase-7-polish.html` | Phase 7 — التقرير (حالة معايير القبول، بصدق: ✅/⏳/❌) |
| `review-2026-08-05-phase-7.html` | Phase 7 — المراجعة الشاملة النهائية عابرة للمراحل — اكتشفت وعالجت تسريب كلمة مرور حرج |
| `pm-log.md` | سجلّ كل خطوة عبر كل المراحل (73 خطوة) |
| `TRADER_NOTES.md` | هذا الملف |

**ملاحظات:**
- بعض المراحل (2-6) دمجت الخطة والتقرير في ملف واحد `plan-phase-N-*.html` بدل ملفَين منفصلين، بطلبك الصريح لتسريع الوتيرة بعد Phase 1. Phase 1 وPhase 7 فقط لهما ملفا `plan-*`/`report-*` بأسماء منفصلة (وPhase 7 تحديداً بلا ملف `plan-phase-7` مستقل — مرحلة تحقق/تلميع لا بناء ميزة جديدة).
- Phase 7 اكتشفت (عبر فحص شامل ومراجعة reviewer الشاملة) أن كلمة مرور admin الفعلية كانت مسرَّبة بالنص الصريح في `review-2026-08-05-phase-1.html`، `review-2026-08-05-phase-3.html`، و`pm-log.md` — رغم أن `plan-phase-1`/`report-phase-1` كانا قد حُجبا سابقاً في نفس الأسبوع. الثلاثة صُحِّحت. **معلَّق:** التحقق من تاريخ git المحلي قبل أي push (انظر §٦ أدناه).

---

## ٦. ما تبقى عليك أنت (يدوياً) قبل أي استخدام حقيقي

1. **🔴 عاجل أولاً:** افحص (بنفسك أو بمساعدة الجلسة المنسِّقة عبر `git log -p pm-log.md`) ما إذا كانت الكوميتات المحلية الحالية تحتوي النسخة القديمة المسرَّبة من كلمة المرور في تاريخها. إن كانت كذلك، نظّف التاريخ (`rebase`/`amend`) **قبل** أي push — بعده يصبح التنظيف أصعب بكثير (يتطلب force-push).
2. **تشغيل `trader-schema.sql`** يدوياً في Supabase Studio → SQL Editor (الملف موجود محلياً على جهازك، مستثنى من Git عمداً).
3. **تغيير كلمة مرور `admin`** فور أول تسجيل دخول ناجح — **إلزامي الآن** (§٢ أعلاه)، ليس اختيارياً.
4. **اختبار محلي أو معاينة (`vercel dev` أو Preview Deployment)** للتأكد أن `vercel.json` لا يكسر أي صفحة موجودة، قبل أي دفع لـ main. لم يُختبَر بعد — لا أداة تشغيل خادم توفّرت لأي جلسة عبر المشروع كله.
5. **موافقتك الصريحة على push لـ main** — لم يحدث أي push حتى الآن، وهذا قرار منفصل تماماً عن باقي المراحل حسب طلبك.
